[build]
builder = "NIXPACKS"

[deploy]
startCommand = "alembic upgrade head && supervisord -c supervisord.conf"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
